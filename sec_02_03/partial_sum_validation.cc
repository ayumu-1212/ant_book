#include <bits/stdc++.h>
using namespace std;
typedef long long lint;

int main() {
  int n, K;
  cin >> n >> K;
  vector<int> a(n), m(n);
  for (int i = 0; i < n; i++) cin >> a[i];
  for (int i = 0; i < n; i++) cin >> m[i];
  vector<vector<int>> results(n+1, vector<int>(K+1, 0));
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < m[i]; j++) {
    }
  }
}
